/** @type {import('postcss-load-config').Config} */
const config = {
    plugins: {
        tailwindcss: {},
        autoprefixer: {}, // Good practice to include explicitly
    },
};

export default config;
